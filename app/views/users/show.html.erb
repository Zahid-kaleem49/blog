<%= @user.inspect %>

<%= render 'layouts/flash' %>
    
            <table>
            <tr>
                <th>Title</th>
                <th>Description</th>
                <th>Created by</th>
                <th>Created at:</th>
                <th>Updated </th>
            </tr>
            
            <% @articles.each do |article|%>
                <tr>
                    <td>
                        <%= article.title %>
                    </td>
                    <td>
                        <%= article.description %>
                    </td>
                    <td>
                        <%= article.user.username %>
                    </td>
                    
                    <td>
                        <%= time_ago_in_words(article.created_at) %>
                    </td>
                    <td>
                        <%= time_ago_in_words(article.updated_at) %>
                    </td>
                    <td>
                    </td>
                    
                </tr>

                <% end %>
                                            Total article : <%= @user.articles.count %>

            
                    </table>
                    <%= @user.email %>,<br>
                       <% if logged_in? %>
                           <%= link_to 'Edit your profile' , edit_user_path(@user.id) %><br>
                            <%= button_to 'logout', logout_path, method: :delete %><br>
                        <% else %>
                            <%= link_to 'login' , login_path %><br>
                        <% end %>
                        